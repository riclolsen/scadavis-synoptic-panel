export interface ScadavisPanelOptions {
  svgUrl: string;
  zoomLevel: number;
  moveByX: number;
  moveByY: number;
  autoResize: boolean;
  showZoomPan: boolean;  
  enableMousePan: boolean;  
  enableMouseWheelZoom: boolean;
}
